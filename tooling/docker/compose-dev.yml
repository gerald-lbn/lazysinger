name: lazysinger-dev

services:
  redis:
    container_name: redis
    env_file:
      - ../../.env.test
    image: "redis:8.2.1"
    command: "redis-server --save 20 1 --loglevel warning"
    volumes:
      - redis_data:/data
    ports:
      - 6379:6379

volumes:
  redis_data:
