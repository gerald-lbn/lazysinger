app:
  name: Refrain
  environment: "local"

database:
  driver: "sqlite3"
  connection: "/data/refrain.db"

libraries:
  paths:
    - "/music"

redis:
  addr: redis:6379

tasks:
  goroutines: 10
  releaseAfter: "15m"
  cleanupInterval: "1h"
  shutdownTimeout: "10s"
