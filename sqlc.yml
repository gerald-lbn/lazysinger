version: "2"
sql:
  - engine: "sqlite"
    schema: "./db/migrations"
    queries: "./db/queries"
    gen:
      go:
        emit_json_tags: true
        package: "repository"
        out: "pkg/repository"
